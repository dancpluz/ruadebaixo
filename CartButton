import React from "react";

    function CartButton({}) {
      return (<button type='button' onClick={''}>
            <ShoppingBagOutlined />
            <span className='cart-item-qty'>{''}</span>
          </button>);
    }
  
  